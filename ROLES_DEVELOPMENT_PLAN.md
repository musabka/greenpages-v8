# 🏗️ خطة تطوير نظام الأدوار والصلاحيات الشامل

**تاريخ الإنشاء:** 4 يناير 2026  
**الحالة:** قيد التنفيذ  
**الأولوية:** حرجة

---

## 📋 الأدوار المطلوبة

### 1. ADMIN (المدير)
**الوصف:** يملك كامل الصلاحيات على النظام
**الصلاحيات:**
- ✅ إدارة جميع المستخدمين والأدوار
- ✅ إدارة الإعدادات التقنية والنظام
- ✅ الوصول لجميع البيانات والتقارير
- ✅ إدارة مديري المحافظات
- ✅ إدارة الباقات والأسعار
- ✅ إدارة قاعدة البيانات الجغرافية
- ✅ مراقبة جميع العمليات

### 2. SUPERVISOR (المشرف)
**الوصف:** نفس صلاحيات المدير ما عدا الأمور التقنية
**الصلاحيات:**
- ✅ إدارة المستخدمين (ما عدا المدراء)
- ✅ إدارة الأنشطة التجارية
- ✅ مراجعة الموافقات والرفض
- ✅ إدارة مديري المحافظات والمندوبين
- ✅ عرض التقارير الشاملة
- ❌ الإعدادات التقنية
- ❌ إدارة الباقات والأسعار
- ❌ تعديل قاعدة البيانات الجغرافية

### 3. GOVERNORATE_MANAGER (مدير المحافظة)
**الوصف:** يدير الأنشطة والمندوبين في محافظات محددة
**الصلاحيات:**
- ✅ إدارة الأنشطة التجارية في محافظاته فقط
- ✅ إدارة المندوبين التابعين له
- ✅ تعيين المهام للمندوبين
- ✅ مراجعة طلبات التجديد في منطقته
- ✅ عرض تقارير منطقته
- ✅ التواصل مع أصحاب الأنشطة في منطقته
- ❌ الوصول لبيانات محافظات أخرى
- ❌ إدارة مديري محافظات آخرين

### 4. AGENT (المندوب)
**الوصف:** مسؤول جمع البيانات الميدانية
**الصلاحيات:**
- ✅ إضافة أنشطة تجارية جديدة
- ✅ إدارة الاشتراكات التي يجلبها
- ✅ متابعة التجديدات المخصصة له
- ✅ عرض عمولاته ومكافآته
- ✅ تسجيل زيارات ميدانية
- ❌ تعديل أنشطة مندوبين آخرين
- ❌ الوصول للإعدادات

### 5. BUSINESS (مالك النشاط)
**الوصف:** يدير نشاطه التجاري
**الصلاحيات:**
- ✅ إدارة معلومات نشاطه
- ✅ إدارة الفروع والمنتجات
- ✅ عرض والرد على التقييمات
- ✅ عرض تحليلات نشاطه
- ✅ إدارة الاشتراك والباقة
- ❌ الوصول لأي بيانات خارج نشاطه

### 6. USER (المستخدم العادي)
**الوصف:** مستخدم الموقع العام
**الصلاحيات:**
- ✅ تصفح الأنشطة التجارية
- ✅ كتابة تقييمات
- ✅ حفظ المفضلة
- ✅ إدارة ملفه الشخصي
- ❌ الوصول لأي لوحة إدارة

---

## 🗂️ هيكل قاعدة البيانات

### جدول جديد: GovernorateManager
```prisma
model GovernorateManager {
  id              String       @id @default(uuid())
  userId          String       @db.Uuid
  governorateId   String       @db.Uuid
  isActive        Boolean      @default(true)
  createdAt       DateTime     @default(now())
  
  user            User         @relation(fields: [userId], references: [id])
  governorate     Governorate  @relation(fields: [governorateId], references: [id])
}
```

### جدول جديد: AgentProfile
```prisma
model AgentProfile {
  id              String       @id @default(uuid())
  userId          String       @unique @db.Uuid
  managerId       String?      @db.Uuid  // مدير المحافظة
  baseSalary      Decimal      @default(0)
  commissionRate  Decimal      @default(0)  // نسبة العمولة %
  totalEarnings   Decimal      @default(0)
  governorateIds  String[]     // المحافظات المسموحة
  isActive        Boolean      @default(true)
  createdAt       DateTime     @default(now())
  
  user            User         @relation(fields: [userId], references: [id])
  manager         User?        @relation("AgentManager", fields: [managerId], references: [id])
  commissions     AgentCommission[]
}
```

### جدول جديد: AgentCommission
```prisma
model AgentCommission {
  id              String       @id @default(uuid())
  agentId         String       @db.Uuid
  businessId      String       @db.Uuid
  amount          Decimal
  rate            Decimal      // نسبة العمولة المطبقة
  type            CommissionType
  status          CommissionStatus @default(PENDING)
  paidAt          DateTime?
  createdAt       DateTime     @default(now())
  
  agent           AgentProfile @relation(fields: [agentId], references: [id])
  business        Business     @relation(fields: [businessId], references: [id])
}

enum CommissionType {
  NEW_SUBSCRIPTION    // اشتراك جديد
  RENEWAL             // تجديد
  UPGRADE             // ترقية
}

enum CommissionStatus {
  PENDING             // معلقة
  APPROVED            // معتمدة
  PAID                // مدفوعة
  CANCELLED           // ملغية
}
```

---

## 📁 هيكل الملفات

```
apps/
├── admin/                    # لوحة الإدارة (ADMIN, SUPERVISOR)
│   └── src/app/(dashboard)/
│       ├── layout.tsx        # فلترة حسب الدور
│       ├── users/            # إدارة المستخدمين
│       ├── businesses/       # إدارة الأنشطة
│       ├── governorates/     # إدارة المحافظات
│       ├── packages/         # إدارة الباقات (ADMIN فقط)
│       ├── settings/         # الإعدادات (ADMIN فقط)
│       └── reports/          # التقارير
│
├── manager/                  # لوحة مدير المحافظة (جديد)
│   └── src/app/(dashboard)/
│       ├── layout.tsx
│       ├── page.tsx          # الإحصائيات
│       ├── businesses/       # الأنشطة في المحافظة
│       ├── agents/           # المندوبين التابعين
│       ├── renewals/         # التجديدات
│       └── reports/          # تقارير المحافظة
│
├── agent/                    # لوحة المندوب (جديد)
│   └── src/app/(dashboard)/
│       ├── layout.tsx
│       ├── page.tsx          # الإحصائيات
│       ├── businesses/       # الأنشطة التي يديرها
│       ├── visits/           # الزيارات الميدانية
│       ├── renewals/         # التجديدات المخصصة
│       ├── earnings/         # العمولات والأرباح
│       └── profile/          # الملف الشخصي
│
├── web/                      # الموقع العام + لوحة المالك
│   └── src/app/
│       ├── business/
│       │   ├── login/        # تسجيل دخول المالك
│       │   └── dashboard/    # لوحة المالك (موجودة)
│       └── ...
│
└── mobile-agent/             # تطبيق المندوب (Flutter)
```

---

## 🔒 نظام الصلاحيات (Guards)

### RolesGuard محسّن
```typescript
@Injectable()
export class RolesGuard implements CanActivate {
  canActivate(context: ExecutionContext): boolean {
    const requiredRoles = this.reflector.get<UserRole[]>('roles', context.getHandler());
    const user = context.switchToHttp().getRequest().user;
    
    // التحقق من الدور
    if (!requiredRoles.includes(user.role)) {
      throw new ForbiddenException('غير مصرح لك بالوصول');
    }
    
    // التحقق من نطاق المحافظة (لمدير المحافظة)
    if (user.role === 'GOVERNORATE_MANAGER') {
      return this.validateGovernorateAccess(user, context);
    }
    
    // التحقق من ملكية النشاط (للمندوب)
    if (user.role === 'AGENT') {
      return this.validateAgentAccess(user, context);
    }
    
    return true;
  }
}
```

### PermissionsMatrix
```typescript
const PERMISSIONS = {
  ADMIN: {
    users: ['create', 'read', 'update', 'delete', 'manage_roles'],
    businesses: ['create', 'read', 'update', 'delete', 'approve', 'reject'],
    packages: ['create', 'read', 'update', 'delete'],
    settings: ['read', 'update'],
    reports: ['all'],
    geography: ['create', 'read', 'update', 'delete'],
  },
  SUPERVISOR: {
    users: ['create', 'read', 'update'], // لا يمكنه حذف أو إدارة الأدوار
    businesses: ['create', 'read', 'update', 'delete', 'approve', 'reject'],
    packages: ['read'], // عرض فقط
    settings: [], // لا وصول
    reports: ['operational'], // تقارير تشغيلية فقط
    geography: ['read'], // عرض فقط
  },
  GOVERNORATE_MANAGER: {
    users: ['read'], // عرض المندوبين التابعين فقط
    businesses: ['read', 'update'], // في محافظاته فقط
    agents: ['create', 'read', 'update', 'assign_tasks'],
    reports: ['governorate'], // تقارير المحافظة
  },
  AGENT: {
    businesses: ['create', 'read', 'update'], // التي يديرها فقط
    visits: ['create', 'read'],
    renewals: ['read', 'update'], // المخصصة له فقط
    earnings: ['read'],
  },
  BUSINESS: {
    profile: ['read', 'update'],
    branches: ['create', 'read', 'update', 'delete'],
    products: ['create', 'read', 'update', 'delete'],
    reviews: ['read', 'reply'],
    analytics: ['read'],
  },
  USER: {
    profile: ['read', 'update'],
    reviews: ['create', 'read'],
    favorites: ['create', 'read', 'delete'],
  },
};
```

---

## 📊 لوحات التحكم

### 1. لوحة المدير (ADMIN)
| القائمة | الوصف |
|---------|-------|
| الإحصائيات | إحصائيات شاملة للنظام |
| المستخدمين | إدارة جميع المستخدمين والأدوار |
| الأنشطة | إدارة جميع الأنشطة |
| المحافظات | إدارة المحافظات ومديريها |
| الباقات | إدارة الباقات والأسعار |
| الإعلانات | إدارة الإعلانات |
| التقييمات | إدارة التقييمات |
| التقارير | تقارير شاملة |
| الإشعارات | إدارة الإشعارات |
| الإعدادات | إعدادات النظام |

### 2. لوحة المشرف (SUPERVISOR)
| القائمة | الوصف |
|---------|-------|
| الإحصائيات | إحصائيات تشغيلية |
| المستخدمين | إدارة (ما عدا المدراء) |
| الأنشطة | إدارة الأنشطة |
| مديري المحافظات | إدارة مديري المحافظات |
| المندوبين | إدارة المندوبين |
| التقييمات | إدارة التقييمات |
| التقارير | تقارير تشغيلية |
| الإشعارات | إرسال الإشعارات |

### 3. لوحة مدير المحافظة (GOVERNORATE_MANAGER)
| القائمة | الوصف |
|---------|-------|
| الإحصائيات | إحصائيات المحافظة |
| الأنشطة | أنشطة المحافظة |
| المندوبين | المندوبين التابعين |
| المهام | توزيع المهام |
| التجديدات | متابعة التجديدات |
| التقارير | تقارير المحافظة |

### 4. لوحة المندوب (AGENT)
| القائمة | الوصف |
|---------|-------|
| الإحصائيات | إحصائياتي |
| الأنشطة | الأنشطة التي أديرها |
| المهام | مهامي اليومية |
| الزيارات | تسجيل الزيارات |
| التجديدات | متابعة التجديدات |
| الأرباح | عمولاتي ومكافآتي |
| ملفي | الملف الشخصي |

### 5. لوحة المالك (BUSINESS) - موجودة
| القائمة | الوصف |
|---------|-------|
| الإحصائيات | إحصائيات النشاط |
| معلومات النشاط | تعديل البيانات |
| الفروع | إدارة الفروع |
| المنتجات | إدارة المنتجات |
| المعرض | معرض الصور |
| التقييمات | الرد على التقييمات |
| التحليلات | تحليلات مفصلة |
| الاشتراك | إدارة الاشتراك |
| الإعدادات | إعدادات الحساب |

---

## 🔄 خطة التنفيذ

### المرحلة 1: تحديث قاعدة البيانات (2 ساعة)
- [ ] تحديث enum UserRole
- [ ] إنشاء جدول GovernorateManager
- [ ] إنشاء جدول AgentProfile
- [ ] إنشاء جدول AgentCommission
- [ ] تشغيل migrations

### المرحلة 2: تحديث Backend (4 ساعات)
- [ ] تحديث Guards و Decorators
- [ ] إنشاء PermissionsService
- [ ] تحديث AuthService
- [ ] إنشاء modules جديدة (governorate-manager, agent-portal)

### المرحلة 3: تحديث Admin Panel (3 ساعات)
- [ ] تحديث sidebar بالفلترة المحسنة
- [ ] تحديث صفحة المستخدمين
- [ ] إضافة صفحة مديري المحافظات

### المرحلة 4: إنشاء لوحة مدير المحافظة (4 ساعات)
- [x] إنشاء تطبيق manager أو route group
- [x] إنشاء الصفحات المطلوبة
- [x] ربط مع Backend

### المرحلة 5: إنشاء لوحة المندوب (4 ساعات)
- [x] إنشاء تطبيق agent أو route group
- [x] إنشاء الصفحات المطلوبة
- [x] نظام العمولات

### المرحلة 6: الاختبار والتحسين (2 ساعة)
- [ ] اختبار كل دور
- [ ] اختبار الصلاحيات
- [ ] تحسين الأداء

---

## ⏱️ الوقت المقدر

| المرحلة | الوقت |
|---------|-------|
| قاعدة البيانات | 2 ساعة |
| Backend | 4 ساعات |
| Admin Panel | 3 ساعات |
| Manager Panel | 4 ساعات |
| Agent Panel | 4 ساعات |
| الاختبار | 2 ساعة |
| **الإجمالي** | **~19 ساعة** |

---

## 🚀 البدء الآن

سأبدأ بتنفيذ المرحلة 1: تحديث قاعدة البيانات
